# Public client settings (exposed to the browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Database connection for Supabase (server-side)
SUPABASE_DB_URL=

# AI provider selection and credentials
# AI_PROVIDER options seen: openai (default), cerebras
AI_PROVIDER=openai
OPENAI_API_KEY=
CEREBRAS_API_KEY=
# Optional: override default Cerebras base URL
CEREBRAS_BASE_URL=

# Course generation worker tuning (all optional; defaults in code)
COURSE_GENERATION_WORKER_IDLE_MS=3000
COURSE_GENERATION_WORKER_ERROR_MS=5000
COURSE_GENERATION_HEARTBEAT_MS=45000
COURSE_GENERATION_STALE_TIMEOUT_MS=180000
COURSE_GENERATION_REQUEUE_INTERVAL_MS=60000
COURSE_GENERATION_WORKER_CONCURRENCY=3

# Optional host identifiers used in worker IDs
RENDER_INSTANCE_ID=
HOSTNAME=
